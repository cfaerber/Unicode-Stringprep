ROADMAP for Unicode::Stringprep

v2.x

- string preperation for stored strings

- "use bytes" to allow filtering/mapping illegal Unicode
  codepoints (e.g.  surrogate pairs U+D800..U+DFFF). Currently,
  this does not work at all with perl versions 5.6.0 to 5.8.2.

$Id$  
